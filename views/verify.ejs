<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css'>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
</head>
  <body class="light-gray-bg">
    <div class="container"> 
        <div class="container-wrapper mainbox">                    
                <div class="panel panel-info" >
                    <div class="panel-heading">
                        <div class="panel-title">
                            <%= panelTitle %>
                        </div>
                    </div>                                 
                    <form class="form-horizontal" role="form" method="post" action="/verify/<%= token %>/<%= lineID %>">
                        <div class="form-horizontal-input-group input-group">
                            <span class="icon">
                                <i class="glyphicon glyphicon-user"></i>
                            </span>
                            <input id="login-username" type="text" class="form-control" name="username" placeholder=<%= usernamePlaceholder %>>
                        </div>
                        <input type="hidden" name="_csrf" value="<%= csrfToken% >">    
                        <div class="form-horizontal-input-group input-group">
                            <span class="icon">
                                <i class="glyphicon glyphicon-lock"></i>
                            </span>
                            <input id="login-password" type="password" class="form-control" name="password" placeholder=<%= passwordPlaceholder %>>
                        </div>
                                                        
                        <div class="input-group">
                            <% if(verified) { %>
                                <div class="alert alert-warning pink-bg">
                                    <p class="red">
                                    <% if(typeof error != "undefined"){ %>
                                            <%= error[0].msg %>
                                    <%}%>
                                    <% if(errors == "localDbError" || errors == "bpmsDbError"){ %>
                                            <%= customError %>
                                    </p>
                                     <%}%>
                                </div>
                            <%}%>   
                        </div>

                        <div class="form-group">
                            <!-- Button -->
                            <div class="col-sm-12 controls">
                                <input class="btn btn-success green" type="submit" value=<%= verifyButtonText %> >
                            </div>   
                        </div>                                                 
                    </form>     
                </div>                     
            </div>       
        </div> 
    </div>
    
</body>
</html>
